@model IEnumerable<ControllRR.Application.Dto.ApplicationUserDto>
@{
   
    ViewData["Title"]="Consulta de Setores";
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
<div class="container-fluid px-4">
    <h1 class="mt-4"></h1>
    <ol class="breadcrumb mb-4">
        <li class="breadcrumb-item"><a href="/">Dashboard</a></li>
        <li class="breadcrumb-item"><a href="/Sectors/GetAll">Usuarios</a></li>
          <li class="breadcrumb-item active">Todos</li>
          <li class="breadcrumb-item active">Filtrar</li>
    </ol>
</div>

<div class="container">
    <div class="card">
        <div class="card-header">
            Data:: -- Usuarios do Sistema ::
        </div>
   
<div class="row">
<div class="col-md-12">

<div class="container">
    <div class="table-responsive">
        <table id="usersTable" class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Username</th>
                    <th>Telefone</th>
                    <th>Email</th>
                    <th>Permissões</th>
                    <th>Ações Disponiveis</th>
                </tr>
            </thead>
            <tbody>
               
            </tbody>
             
        </table>
    </div>
</div>
</div>
</div>