@* Views/Shared/Components/SupplierProducts/Default.cshtml *@
@model IEnumerable<ControllRR.Application.Dto.StockDto>

<table class="table">
    <thead>
        <tr>
            <th>Produto</th>
            <th>Quantidade</th>
            <th>Preço</a></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var product in Model)
        {
            <tr>
                <td>@product.ProductName</td>
                <td>@product.ProductQuantity</td>
                <td>@product.PurchasePrice</td>
            </tr>
        }
    </tbody>
</table>